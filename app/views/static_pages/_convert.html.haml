.modal.fade{id: "convert_leads_modal", tabindex: "-1", role: "dialog", aria: {labelledby: "convert_leads_modal_label"}, aria: {hidden: "true"}}
  .modal-dialog
    .modal-content
      .modal-header
        %button{ type: "button", class: "close", data: {dismiss: "modal"}, aria: {hidden: "true"}}×
        %h1.logo.center
          = precede "Invest" do
            %span.orange dex
      .modal-body
        %h4{class: "modal-title", id: "convert_leads_modal_label"} Some sort of tagline.
        %p Second reason. Please sign up for full benefits and access.
        .center= link_to "Sign Up", new_user_registration_path, class: "btn btn-primary btn-large btn-block"
        %br
      .modal-footer
        %p.small.text-right
          Already have an account?
          = succeed "!" do
            = link_to "Sign In", new_user_session_path
= unless user_signed_in?
  :javascript
    $('#convert_leads_modal').modal('show')